# Agent Notes

Use the canonical Python package `src/nocturnix/` and the single UI location `src/nocturnix/static/`. Keep this milestone development-only, mock-only, and in-memory. Do not add credentials, live provider calls, or durable SQL persistence for v0.1.2.
