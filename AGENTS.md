# Agent Notes

Use the canonical Python package `src/nocturnix/` and the single UI location `src/nocturnix/static/`. Keep this milestone development-only and mock-only. For v0.1.3, SQL-backed SQLite persistence and Alembic migrations are allowed, but do not add credentials, live provider calls, OAuth token storage, destructive migrations, or production claims.
