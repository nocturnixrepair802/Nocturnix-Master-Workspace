const CACHE='nocturnix-dev-shell-v012';const STATIC=['/','/manifest.webmanifest','/static/icon.svg'];self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(STATIC))));self.addEventListener('fetch',event=>{const url=new URL(event.request.url);if(url.pathname.startsWith('/api/')||url.pathname.includes('approval')||url.pathname.includes('audit')||url.pathname.includes('email')||url.pathname.includes('repair'))return;event.respondWith(caches.match(event.request).then(cached=>cached||fetch(event.request).catch(()=>caches.match('/'))));});
